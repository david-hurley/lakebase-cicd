bundle:
  name: david-lakebase-cicd-testing

variables:
  project_id:
    description: "Lakebase project ID"

resources:
  postgres_projects:
    lakebase:
      project_id: ${var.project_id}
      display_name: 'David Lakebase CICD'
      pg_version: 17
      history_retention_duration: "1209600s"
      default_endpoint_settings:
        autoscaling_limit_min_cu: 0.5
        autoscaling_limit_max_cu: 2
        no_suspension: true

  postgres_endpoints:
    production_read_replica:
      parent: projects/${var.project_id}/branches/production
      endpoint_id: read-replica
      endpoint_type: ENDPOINT_TYPE_READ_ONLY
      autoscaling_limit_min_cu: 0.5
      autoscaling_limit_max_cu: 2
      suspend_timeout_duration: "300s"
