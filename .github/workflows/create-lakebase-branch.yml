# ---------------------------------------------------------------------------
# Create Lakebase Branch
#
# Triggered whenever a new Git branch is created.  Creates a matching
# Lakebase dev branch (forked from production) with its own read-write
# endpoint so each feature branch gets an isolated database environment.
#
# The branch name is sanitised to satisfy Lakebase naming rules
# (lowercase alphanumeric + hyphens, max 63 chars).
#
# The branch has a 7-day TTL and is automatically deleted if the
# corresponding Git branch is removed (see delete-lakebase-branch.yml).
# ---------------------------------------------------------------------------

name: Create Lakebase Autoscaling Branch

on:
  create:

jobs:
  create-lakebase-autoscaling-branch:
    runs-on: ubuntu-latest
    env:
      DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
      PROJECT_NAME: ${{ vars.LAKEBASE_PROJECT_NAME }}

    steps:
      - name: Install Databricks CLI
        uses: databricks/setup-cli@main

      - name: Format branch name
        id: branch
        run: |
          SANITIZED=$(echo "${{ github.event.ref }}" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9-]/-/g' | sed 's/^-//' | sed 's/-$//' | cut -c1-63)
          echo "name=$SANITIZED" >> "$GITHUB_OUTPUT"

      - name: Create Lakebase branch
        run: |
          databricks postgres create-branch \
            "projects/$PROJECT_NAME" \
            "${{ steps.branch.outputs.name }}" \
            --json '{
              "spec": {
                "source_branch": "projects/'"$PROJECT_NAME"'/branches/production",
                "ttl": "604800s"
              }
            }'

      - name: Create read-write endpoint
        run: |
          databricks postgres create-endpoint \
            "projects/$PROJECT_NAME/branches/${{ steps.branch.outputs.name }}" \
            primary \
            --json '{
              "spec": {
                "endpoint_type": "ENDPOINT_TYPE_READ_WRITE",
                "autoscaling_limit_min_cu": 0.5,
                "autoscaling_limit_max_cu": 2.0
              }
            }'
